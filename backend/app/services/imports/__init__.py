"""Spreadsheet import pipeline: bytes in, tenant data out, in stages.

Task 3 lives in `table.py` and stops at rows-as-dicts — no row parsing, no
matching, no database. Later tasks build on top of this module rather than
reaching around it.
"""
